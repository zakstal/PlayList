name space nextVideo in VideoHome
nextVideo problem, is not swiching viedoes
add domeInterface.js move addTitlesToScreen into it
add viewed-video-container and upcoming-video-container to the index.html
took out
 if (typeof video !== "undefined") {
 if (idx === 0) {
 	currentVideo = video;
 } else {

 from putVideosInArray and added if (typeof currentVideo !== "undefined") playedVideos.push(justPlayedVideo);

 in

 VideoHome.nextVideo = function() {
	var upComingVideo = videos.shift();
	var justPlayedVideo = currentVideo;
	if (typeof currentVideo !== "undefined") playedVideos.push(justPlayedVideo);
	currentVideo = upComingVideo;
}